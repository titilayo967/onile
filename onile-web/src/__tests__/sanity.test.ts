import { describe, it, expect } from 'vitest'

describe('sanity', () => {
  it('basic arithmetic works', () => {
    expect(1 + 1).toBe(2)
  })

  it('sanity check', () => {
    expect(true).toBe(true)
  })
})
